{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_widget(form) }}
    <div>

        <br>

        <input type="submit" class="btn btn-default btn-lg btn-download" value="{{ 'registration.submit'|trans }}" />

        <br><br>

        <p><a href="../login" class="btn btn-default btn-lg btn-download">Retour à la page précédente</a></p>

    </div>
{{ form_end(form) }}
