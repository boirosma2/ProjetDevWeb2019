{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'label.ShowProject'|trans }}</h1>

    <table>
        <tbody>
            <tr>
                <th>{{ 'label.Id'|trans }}</th>
                <td>{{ projet.id }}</td>
            </tr>
            <tr>
                <th>{{ 'label.Name'|trans }}</th>
                <td>{{ projet.nom }}</td>
            </tr>
            <tr>
                <th>{{ 'label.Description'|trans }}</th>
                <td>{{ projet.description }}</td>
            </tr>
            <tr>
                <th>{{ 'label.DateLine'|trans }}</th>
                <td>{% if projet.dateFin %}{{ projet.dateFin|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>{{ 'label.StartdDate'|trans }}</th>
                <td>{% if projet.dateDeb %}{{ projet.dateDeb|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>{{ 'label.Client'|trans }}</th>
                <td>{{ projet.client }}</td>
            </tr>
            <tr>
                <th>{{ 'label.Budget'|trans }}</th>
                <td>{{ projet.budget }}</td>
            </tr>
            <tr>
                <th>{{ 'label.Task'|trans }}</th>
                <td>
                    {% for tache in projet.taches %}
                        {{ tache.nom }}<br>
                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('projet_index') }}">{{ 'label.BackToList'|trans }}</a>
        </li>
        <li>
            <a href="{{ path('projet_edit', { 'id': projet.id }) }}">{{ 'label.Edit'|trans }}</a>
        </li>
        <li>
            <a href="{{ path('tache_new', { 'id': projet.id }) }}">{{ 'label.AddTask'|trans }}</a>
        </li>
        <li>
            <a href="{{ path('tache_showAllFromProjet', { 'id': projet.id }) }}">{{ 'label.ShowTaskProject'|trans }}</a>
        </li>

        tache_showAllFromProjet

        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
