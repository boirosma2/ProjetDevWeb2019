{% extends 'base.html.twig' %}

{% block body %}
<div class="col-sm-6">
    <h1>{{ 'label.TaskCreate'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input  class="btn btn-success" type="submit" value={{ 'label.AddTask'|trans }} />
    {{ form_end(form) }}


            <a href="{{ path('tache_index') }}">{{ 'label.BackToList'|trans }}</a>
</div>
{% endblock %}
